cmake_minimum_required(VERSION 3.14.0)

project(cppzmq_playground)

set(root_dir ${CMAKE_CURRENT_SOURCE_DIR})

if(WIN32)
    set(deps_dir E:/work/SimReposity/SimTools/deps)
    set(inc_dirs ${deps_dir}/cppzmq/include ${deps_dir}/ZeroMQ/include ${root_dir})
    set(lib_dirs ${deps_dir}/cppzmq/lib ${deps_dir}/ZeroMQ/lib)

    set(lib_zmq libzmq-v143-mt-s-4_3_5.lib)
else()
    set(inc_dirs /usr/include /usr/local/include)
    set(lib_dirs /usr/lib /usr/local/lib /usr/bin /usr/local/bin)

    set(lib_zmq zmq)
endif(WIN32)

include(${inc_dirs})
link_directories(${lib_dirs})

aux_source_directory(${root_dir} src_files)
